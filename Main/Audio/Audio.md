##The Audio() constructor creates and returns a new HTMLAudioElement(like our html) which can be either attached to a document for the user to interact with and/or listen to, or can be used offscreen(like our js) to manage and play audio.


new Audio(url)

 method that we can use

.play() for playing audio
.pause() for pausing audio
.load() for loading audio again
.addTextTrack() adding text to the audio
.fastSeek(time) jumping to certain time
.canPlayType(type) checking if we can play the audio

property 

.src
.ended  is ended?
.paused is paused?
.loop atumatic loop
.volume
.muted
.autoplay

example in js file